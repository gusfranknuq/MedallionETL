bundle:
  name: supply_chain_bootcamp

include:
  - resources/*.yml

variables:
  workspace_host:
    description: Azure Databricks workspace URL
    default: https://<your-azure-databricks-workspace-url>

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: ${var.workspace_host}
